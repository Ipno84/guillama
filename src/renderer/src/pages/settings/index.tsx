import { SectionTitle } from '@renderer/features/section-title'

export const Settings = (): React.JSX.Element => {
  return (
    <>
      <SectionTitle
        title="Settings"
        hint="Change your settings and personalize your Guillama experience."
      />
    </>
  )
}
