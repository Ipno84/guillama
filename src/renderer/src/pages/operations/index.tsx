import { SectionTitle } from '@renderer/features/section-title'

export const Operations = (): React.JSX.Element => {
  return (
    <>
      <SectionTitle
        title="Operations"
        hint="To keep track of downloads and all the operations running in the background"
      />
    </>
  )
}
